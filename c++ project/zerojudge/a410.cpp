#include <iostream>
using namespace std;

int main()
{
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    if (a/d == b/e)
    {
        if (a/d == c/f)
        {
            cout << "Too many" <<'\n';
        }
        cout << "No answer" <<'\n';
    }
    else{
        
    }
    return 0;
}